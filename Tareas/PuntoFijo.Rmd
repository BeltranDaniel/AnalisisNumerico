---
title: "R Notebook"
output: html_notebook
---

```{r}
f <- function (x) {
  return (exp(x) - pi*x)
}

g <- function (x) {
  return (exp(x)/pi)
}

puntofijo <- function(a,b){
  
  E<-1
  error<-1e-4
  
  while(error < E)
  {
    x1 <- g(x)
    
    if(x != x1){
      x <- x + pasos
    }
    
    E <- abs(x1 -x)/abs(x1)
    
  }
  return (x)
}

x<-0
x1<-0
pasos<-2/100000

x<- puntofijo(0,1)
cat("Primera raíz", x, g(x))

x<-1
x<- puntofijo(1,2)
cat("Segunda raíz", x, g(x))

```

```{r}
plot(f)
```

